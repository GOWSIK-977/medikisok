# mock = no API calls, deterministic canned logic (default, works with zero setup)
# anthropic = real LLM calls (fill in ANTHROPIC_API_KEY and implement the TODOs in src/llm/anthropicProvider.js)
LLM_PROVIDER=mock

ANTHROPIC_API_KEY=
PORT=4000
